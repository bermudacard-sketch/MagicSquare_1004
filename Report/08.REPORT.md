# MagicSquare_1004 — REFACTOR Safe · G1 SSOT 단일화

| 항목 | 내용 |
|------|------|
| 프로젝트 | MagicSquare_1004 (4×4 마방진, 10선 검증) |
| 세션 | 08 |
| Phase | refactor |
| ARRR 사이클 | Refine — `/refactor-smell` (⑦) → `/refactor-safe` (⑧) |
| 보고서 생성일 | 2026-06-25 |
| Test ID | (회귀) T-02, D-LOC-01, D-SOL-01, U-IN-01, U-IN-02 |
| Command | `/refactor-smell`, `/refactor-safe` |

관련 Transcript: [Prompting/08.Export-Transcript.md](../Prompting/08.Export-Transcript.md)

---

## 1. 요약

`/refactor-smell`로 Logic+UI 스멜 탐지 후 **P1 Duplicated Code — G1 격자 이중 정의** 1건만 `/refactor-safe`로 처리했다. `src/entity/grids.py`에 `GRID_G1` SSOT를 두고, `src/boundary/grid_g1.py`(re-export)와 `tests/conftest.py`(import + shallow copy)가 참조하도록 정리했다. 격자 값·빈칸 좌표·GUI 초기 격자·`grid_g1` 픽스처 동작·Golden 출력 **불변**. pytest **5 passed**, D-LOC-01 Golden **matched**.

---

## 2. 입력 스냅샷 (Step A)

| 항목 | 값 |
|------|-----|
| git status | `refactoring` 브랜치 · `grid_g1.py`, `conftest.py`, `validate_lines.py` 수정 · `src/entity/grids.py` untracked |
| pytest (시작) | `python -m pytest tests/ -v` → **5 passed** |
| Phase 선언 | `Phase: refactor \| Layer: boundary \| Track: UI` |
| Command | `/refactor-safe` — 후보 #1 G1 SSOT |

---

## 3. STEP — RED

해당 없음

---

## 4. STEP — GREEN

해당 없음

---

## 5. STEP — REFACTOR

### 5.1 `/refactor-smell` (⑦)

| 항목 | 내용 |
|------|------|
| 범위 | `src/` · `tests/` — Logic+UI |
| P0 | 없음 (Magic Number·ECB 위반 없음) |
| P1 | G1 SSOT 중복, `_collect_failed_line_ids` 중복, `_build_ui` Long Method |
| P2 | `blank_coords_0` 명명, `solver_step_a` 이중 루프, `_read_grid` Feature Envy |
| 후보 #1 | G1 격자 SSOT 단일화 |

### 5.2 `/refactor-safe` (⑧) — 후보 #1

| 항목 | 내용 |
|------|------|
| 스멜 | P1 Duplicated Code — `GRID_G1` 이중 정의 |
| Before | `src/boundary/grid_g1.py` · `tests/conftest.py` 각각 G1 리터럴 복제 |
| After | `src/entity/grids.py:GRID_G1` SSOT → boundary re-export · conftest import |
| ECB | entity → boundary import 없음 · boundary → entity 허용 |
| Budget | 파일 3 · 메서드 0 (≤3 준수) |
| 불변 | 격자 값, `int[6]`, golden 좌표, E002/E003, `validate_lines` 계약 |

### 5.3 격자 일치 검증

| 소스 | 내용 |
|------|------|
| `src.entity.grids.GRID_G1` | `[[16,3,2,13], [5,10,0,8], [9,6,7,12], [4,15,14,0]]` |
| `src.boundary.grid_g1.GRID_G1` | 동일 객체 (`is` 확인) |
| `app.py` GUI | `boundary.grid_g1.GRID_G1` 사용 — 초기 격자 동일 |
| `grid_g1` 픽스처 | `[row[:] for row in GRID_G1]` — 값 동일, 독립 복사 유지 |

---

## 6. STEP — REPEAT

해당 없음

---

## 7. 생성·변경 파일

| 파일 | 작업 | 비고 |
|------|------|------|
| `src/entity/grids.py` | 신규 | G1 `GRID_G1` SSOT |
| `src/boundary/grid_g1.py` | 수정 | entity re-export shim |
| `tests/conftest.py` | 수정 | `GRID_G1` import, shallow copy 반환 |
| `Report/08.REPORT.md` | 신규 | 본 문서 |
| `Prompting/08.Export-Transcript.md` | 신규 | Transcript Export |
| `README.md` | 수정 | 진행 상태·문서 표 08행 |

---

## 8. pytest 상태 (최종)

```text
python -m pytest tests/ -v
→ 5 passed

python -m pytest tests/boundary/ -v
→ 2 passed

python -m pytest tests/entity/test_d_loc_01.py -v
→ 1 passed (golden matched, UPDATE_GOLDEN 없음)
```

---

## 9. 다음 단계

1. **`/refactor-smell`** 재실행 — 잔여 P1 (#2 10선 합산 헬퍼, #3 `_build_ui` 분리)
2. D-SOL-01 `/golden-master` — `d_sol_01_g1_step_a.approved.txt`
3. T-01, incomplete, T-03 등 추가 RED

---

*본 문서는 Report/08.REPORT.md — REFACTOR Safe · G1 SSOT 단일화 (2026-06-25)입니다.*
